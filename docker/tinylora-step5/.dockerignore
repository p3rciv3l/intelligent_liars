# Fail closed: the image receives only the explicit build inputs below. Never
# send repository history, .env files, credentials, datasets, or model caches to
# the Docker daemon.
**
!Dockerfile
!requirements.in
!requirements.lock
!runtime-manifest.json
!validate_runtime.py
!write_sbom.py
!third_party/
!third_party/flash-attention-LICENSE
