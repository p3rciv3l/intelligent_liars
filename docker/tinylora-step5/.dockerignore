# Fail closed: the image needs exactly four build inputs. In particular, never
# send repository history, .env files, credentials, datasets, or model caches to
# the Docker daemon.
**
!Dockerfile
!requirements.in
!requirements.lock
!runtime-manifest.json
!validate_runtime.py
