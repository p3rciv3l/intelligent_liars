# Run Optuna without a separate fixed scientific screen

There is no separately executed fixed causal screen or activation-backend pilot before routine optimization. Persistent writer arithmetic, reversibility, module discovery, recipe coverage, resume behavior, model loading, batching, and throughput are hardened through local tests and operational fail-closed checks, after which the authorized persistent-weight study begins directly; bounded activation restoration, re-ablation, false-trigger, random-direction, and related controls remain finalist evidence.
