# Simple truth-direction run plan

**Status:** software contracts and CPU tests are ready. Main does not yet contain
a reviewed end-to-end real-Qwen runner or an execution-ready frozen packet. No
paid run is authorized by this document.

1. Generate a frozen base-only layer-21 last-token probe-score calibration receipt; derive `M`, `B`, and the `B/2B/B` movement caps without inspecting candidate outputs.
2. Bind the pushed source revision/tree, exact model/cache/runtime receipts, calibration hashes, and measured BF16 identity-hook tolerances into the self-hashed contract.
3. Implement and review the smallest real-Qwen runner and stop-only watchdog,
   then dry-run all eight intervention arms plus the shared unchanged base on a
   qualified target runtime.
4. Execute all three specified seeds concurrently without scientific early
   stopping. Instantiate the shared 990 behavior, 51 preservation, and 450
   XSTest inventories for every condition and seed, and preserve the five-probe
   measurement requests. The behavior inventory alone is 26,730 generated rows.
5. Produce external grader/probe receipts bound to those exact requests, resume scoring without rerunning model outputs, and compare only after every arm is terminal and every receipt is present.
6. Upload exact-version artifacts and SHA-256 receipts to versioned S3, re-fetch and verify them, then stop/pause every retained instance.

An arm is `scientific_pass` only if it improves the conditioned false-report behavior on both IID and held-out families, preserves the five linked truthful/action controls, generation quality, capability, and safety, passes all three seeds and uncertainty gates, and is supported by the independent probe diagnostic. Any failed conjunctive gate is `scientific_no_go`; missing/corrupt/incomplete evidence is not a scientific negative.
