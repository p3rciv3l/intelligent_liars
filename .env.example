# Required for OpenRouter LLM judging
OPENROUTER_API_KEY=

# Required for the coordinator-owned live optimization dashboard
WANDB_API_KEY=

# Optional if Hugging Face model download requires authentication
# HF_TOKEN=

# Optional when AWS credentials are selected through a named local profile.
# AWS_PROFILE=
